# run.007 analysis — GATE FAILED, but in the most informative way possible

*Analyzed 2026-07-09. Notebook: `runs/btc_lstm.run.007.ipynb`, executed on Colab. Scores/model on Drive `btc_lstm_run007/`.*

**TLDR: The heads found the moves better than anything built so far (lift 9.4–9.8×, 4/4 folds), but fees eat every configuration — sim net is −10 to −13 bps with CIs entirely below zero. The pre-registered failure-mode guide points at the one lever left: maker entry. And this was v1-compat mode again — the 5th run in a row where the tar had no out3/out4 files.**

## Run setup (as executed)

- 1,679 files, 1.61M rows, 2025-09-15 → 2026-07-04; 2/29 schema-3/4 features present → **v1-compat mode** (39/76 features kept).
- 1.20M valid samples (76.5% — MAX_H 240→8 freed forward-contiguity samples as designed).
- HORIZONS [2,3,4,5,6,8], per-h θ: 15bp (h2,h3) / 20bp (h4–h8); H_SEL=6 primary; 12 BCE opportunity heads; 3 seeds × 4 purged walk-forward folds; rolling-causal τ calibration (14d trailing).
- Exit sim: TP limit at ±θ filled maker on close trade-through (+θ·1e4 − 7bp), SL taker at breaching close, else time-stop taker at h.

## Gate verdict (h6 @ 0.1%, rolling ens, TP=SL=θ)

| criterion | up6 | dn6 |
|---|---|---|
| A: sim net > 0, CI > 0 | −10.04 bps, CI [−12.2, −8.9] — **fail** | −12.93 bps, CI [−14.6, −11.3] — **fail** |
| B: lift ≥3 pooled, ≥2× in ≥3/4 folds | 9.37×, 4/4 — **pass** | 9.81×, 4/4 — **pass** |
| C: neighbour rates sim > 0 | both negative — **fail** | both negative — **fail** |

This maps exactly to failure mode #2 from the notebook's own pre-registered guide: *"lift good, sim ≤ 0, TP-grid all ≤ 0 → touches exist but fees eat them even with a maker TP exit; the remaining lever is entry price (maker entry)."* The entire exploratory TP/SL grid sits in −9.6 to −12.9 bps — no row positive.

## What worked (three design goals, all delivered)

1. **The selective objective genuinely ranks.** Hit 26.5%/27.1% (up/dn) vs 2.8% base at 0.1%; the frozen-regression control under the identical rolling-τ trigger rule gets 4.3%/1.5% — the BCE heads are not re-labelled regression. Seed-unanimity up6 @0.01% reaches **hit 49.7%, lift 17.6×** (n=199).
2. **Rolling-τ calibration fixed run.006's pathology.** Fold 1 fired 208 triggers instead of zero; every fold trades at roughly the nominal rate.
3. **Short-horizon IC is the strongest signal in the project's history.** IC_h2 pooled +0.038 with daily t = **+12.9**, positive in all 4 folds, decaying monotonically to ~0 at h8 (+0.038 → +0.003). Real 30-second predictability — v1 microstructure features *do* see 30–90s, contrary to the "features exhausted" reading from runs 003–005. It is fast-dying mean-reversion-scale signal, far below the 10bp taker floor as a standalone.

## Why it still loses money — the arithmetic, made explicit

A hit pays θ − 7bp = **+13 bps**. The average non-hit costs ≈ **−18.4 bps** (SL exits ≈ −30 incl. fees, time-stops ≈ −10 + drift). Breakeven hit rate ≈ **58.5%**; the best variant achieved 49.7%. No trigger rate, horizon, unanimity filter, or TP/SL multiple closes that gap with taker entry. The non-overlap sim confirms it bluntly: up6 @0.1% ens = 610 trades, −$48.3k, −$79/trade, win 34%.

Two corollaries:

- **The TP-at-θ exit — run.006's #1 post-mortem lever — is now tested and adds essentially nothing at h≤6.** Sim ≈ endpoint (−10.0 vs −9.4 bps). At short horizons touches survive to the endpoint anyway (as the 8h local sample predicted: 89–98% survival), so the maker TP only saves ~3bp of fees while the SL locks in −30bp on adverse paths. The run.006 h40 hit↔net gap will NOT be fixed by exit rules alone.
- **Full-maker economics is borderline-viable.** At 4bp RT the hit pays +16bp and breakeven drops to ≈49% — right at the unanimity head's 49.7%. The maker-entry sim (run.008) is not a hail mary; it is within ~1 hit-rate point, on a feature set that is about to improve.

## Instability note

Fold 2's hit rate collapses to 8.6%/9.0% at 0.1% (other folds 22–40%) while its lift stays ~8–9× — the tail signal is regime-dependent. Any maker-entry pass needs per-fold checks, not just pooled numbers.

## Next steps, in order of value

1. **Fix the tar.** Five consecutive runs have silently tested v1 features only (`EXCLUDED ... 0.0%` for every v3/v4 column in Cell 5). Every conclusion above is about v1 features; the schema-3/4 hypothesis (OFI, walls, liq counts, ETH lead-lag as cascade-onset features) remains completely untested — and it is the most plausible route from 49.7% → 58%+ hit (or to viability at maker ~49%).
2. **run.008 = maker-entry sim + 1-bar entry-delay stress** (the planned follow-up), on schema-3/4 data if the tar is fixed first — one run can do both.
3. Parking-lot idea: the h2 signal (t=12.9) is too small to trade through taker fees standalone but is a natural **entry-timing/execution signal** for the maker-entry sim — use it to decide when to post the limit order.
